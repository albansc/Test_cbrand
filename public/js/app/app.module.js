'use strict';

(function(){
    angular.module('esencia', ['textAngular','angular-carousel']);
/* function getClassifiers() {
  return $.get('/api/classifiers');
};

function removeClassifier(id) {
  return $.post('/api/delete_classifier/' + id);
};*/
})();